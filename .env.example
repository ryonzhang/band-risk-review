# ---- Band platform (same for everyone) ----
THENVOI_REST_URL=https://app.band.ai/
THENVOI_WS_URL=wss://app.band.ai/api/v1/socket/websocket

# ---- LLM provider that powers the agents' reasoning ----
# Use Anthropic, OR point at AI/ML API's unified endpoint to qualify for the
# "Best Use of AI/ML API" partner prize (grab the promo code at kickoff).
ANTHROPIC_API_KEY=sk-ant-your-key-here
# OPENAI_API_KEY=sk-your-key-here
